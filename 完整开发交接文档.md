# 心屿 XINYU：完整开发交接文档

> 交接日期：2026-08-01\\
> 代码基线：`b71485e feat: add Xinyu desktop companion foundation`\\
> 协作分支：`agent/xinyu-desktop-foundation`\\
> 目标硬件基线：本文件第 3 节的 RTX 4070 Ti SUPER 台式机。笔记本仅用于继续编码、调试 UI 和轻量测试，**不作为最终模型性能与数字人质量的验收基准**。

## 1. 项目是什么、现在到了哪里

心屿（XINYU）是一个“本地优先”的陪伴式桌面智能体。目标不是普通聊天窗口，而是一个 16:9 的温暖写实桌面空间：角色、低延迟语音、长期记忆、个人计划、音乐与提醒在同一个桌面应用中工作。

当前阶段已经完成“可演示应用骨架 + 本地核心业务闭环”，尚未完成“真人般实时说话数字人”。

| 能力 | 当前状态 | 已完成内容 | 尚未完成内容 |
| --- | --- | --- | --- |
| 16:9 主 UI | 可用 | 五页导航、人物场景、右侧卡片、底部音乐/输入/情绪坞、真实交互 | 组件进一步拆分、无障碍细化 |
| 对话 | 可用但为开发回退 | 消息持久化、历史恢复、Provider 接口 | 安装并基准测试真实本地 LLM |
| 长期记忆 | 可用 | SQLite、来源、置信度、编辑、置顶、删除、FTS5 搜索、冲突替代关系 | 向量召回、摘要压缩 |
| 计划与提醒 | 可用 | CRUD、到点提醒、安静时段、通知收件箱、温柔问候规则 | 原生系统通知与第三方日历 |
| 音乐 | 可用 | 本地多文件队列、播放控制、自动续播、进度与静音 | 持久媒体库、封面元数据 |
| 实时语音 | 接入层已完成 | Realtime WebSocket 代理、AudioWorklet、麦克风权限、转写持久化、打断机制 | 安装 STT / LLM / TTS 权重并进行真实声学验收 |
| 动态人物 | 框架已完成 | 人物状态机、视频资产检测、安全媒体路由、前端淡入播放与静态回退 | 授权素材、状态视频、口型/表情实时驱动 |
| 桌面安装包 | 工程已完成 | Tauri 壳、托盘、通知、开机启动能力、PyInstaller Core sidecar | 安装 Rust/Cargo 后编译和签名验收 |

当前自动回归：Core 35 项通过（Python 覆盖率 88%）、UI 15 项通过、Sites 4 项通过；Core sidecar 已独立健康检查通过。

## 2. 仓库、分支与另一台电脑的接手方式

- GitHub：<https://github.com/zuming58/AIgirl>
- 当前交接分支：`agent/xinyu-desktop-foundation`
- 草稿 PR：<https://github.com/zuming58/AIgirl/pull/1>
- 上游实时语音子模块：`third_party/speech-to-speech`，固定提交 `656099afffda445a3a3cef8ffee55871c9b6123c`

另一台电脑执行：

```powershell
git clone --recurse-submodules https://github.com/zuming58/AIgirl.git
cd AIgirl
git switch --track origin/agent/xinyu-desktop-foundation
powershell -ExecutionPolicy Bypass -File .\scripts\setup.ps1
powershell -ExecutionPolicy Bypass -File .\scripts\dev.ps1 -Open
```

启动地址固定为：

- UI：`http://127.0.0.1:4173`
- Core：`http://127.0.0.1:8765`
- Core OpenAPI：`http://127.0.0.1:8765/docs`

运行产生的 SQLite、日志、缓存位于 `temp/runtime/`，已被 Git 忽略。`apps/desktop-ui/src-tauri/binaries/` 也是构建产物，不提交。

## 3. 最终目标主机配置（以本机实测为准）

| 项目 | 实测配置 |
| --- | --- |
| 厂商 / 型号 | ASUS / `System Product Name`（台式机） |
| 系统 | Windows 11 家庭中文版，10.0.22631，Build 22631 |
| CPU | Intel Core i7-14700KF，20 核 / 28 线程 |
| 内存 | 31.8 GB（标称 32 GB） |
| GPU | NVIDIA GeForce RTX 4070 Ti SUPER，16,376 MiB（16 GB） |
| NVIDIA 驱动 | 591.44 |
| 当前显示器 | 3840 × 2160 |
| Node / npm | v26.4.0 / 11.17.0 |
| Python | 3.11.15 |
| Git | 2.54.0.windows.1 |
| 工作盘 | F: 963 GB，交接时约 590.7 GB 可用 |

交接时 GPU 可用显存为约 14,466 MiB。最终产品以“16 GB 显存、32 GB 内存”规划；不要把 RTX 5090 的“所有模型同时常驻”策略直接搬过来。

笔记本建议只承担：代码开发、UI、Core API、SQLite、自动化测试、静态人物和开发回退 Provider。若笔记本显存不足，则不要下载/运行完整 STT + LLM + TTS + 口型链路；真实模型基准与人物运行时在本机完成。

## 4. 技术架构

```text
React / Vite UI（Tauri WebView）
    ├─ HTTP / WebSocket → FastAPI Core
    │                      ├─ SQLite WAL + FTS5
    │                      ├─ 对话 Provider / 记忆 / 计划 / 通知
    │                      ├─ Realtime Voice Proxy
    │                      └─ Avatar 状态与安全媒体服务
    ├─ 本地音乐播放
    └─ Tauri：托盘 / 通知 / 开机启动 / sidecar 生命周期

Realtime Voice 独立环境：VAD → STT → LLM → 分句 → TTS → 音频输出
                                               └→ Avatar Worker（局部口型）
```

| 层 | 已选技术 | 说明 |
| --- | --- | --- |
| 前端 | React 19 + Vite 6 | 16:9 UI；开发端口固定 4173 |
| 桌面壳 | Tauri 2 | 无边框窗口、托盘、通知、开机启动；本机尚未安装 Rust/Cargo |
| 后端 | Python 3.11 + FastAPI + Pydantic | 只监听本机回环地址 |
| 数据 | SQLite WAL + FTS5 | 本地优先、可导入导出、备份恢复 |
| 语音媒体 | Hugging Face `speech-to-speech` | 独立运行环境，Core 只做代理与状态编排 |
| 角色呈现 | 状态视频 + LivePortrait + MuseTalk（计划） | 不采用 2.5D / Live2D 作为主路线 |
| 打包 | PyInstaller sidecar + Tauri | 最终用户不应安装 Python |

## 5. 产品与视觉不可变约束

这些是已经确认的设计决定，接手开发不要轻易改动：

1. 品牌名为“心屿 XINYU”，“屿”是岛屿的屿；Logo 是小型通透玫紫水晶心，只有内部心跳线轻微双跳，不出现对话气泡。
2. 主界面必须是 16:9；顶部导航胶囊位于整个屏幕水平中心，不向左侧内容区对齐。
3. 画面主角是温馨桌前的成年年轻女性与房间，而不是工具面板；角色需要位于全屏中心附近，UI 不遮挡脸和上身。
4. 保留桌面叙事：笔记本在前臂下方、右前景杯子、杯后多肉、暖台灯、左侧照片墙、右侧雨夜窗景与半透明卡片。
5. 底部是扁平圆角胶囊：音乐、单一语音/文字输入、情绪球。不要再加“随便聊聊”等快捷按钮。
6. 默认人物路线是高质量写实状态视频和局部脸部口型；显存不足时回退到预制写实说话视频，**不回退到 2.5D**。

更完整的视觉说明见 `apps/desktop-ui/AGENTS.md`、`docs/design/页面与交互规划.md`、`docs/design/UI-设计还原与验收记录.md`。

## 6. 代码目录与职责

```text
apps/desktop-ui/
├─ src/App.jsx                   # 五个页面与当前主要 UI 交互
├─ src/styles.css                # 16:9 场景和组件样式
├─ src/services/coreApi.js       # HTTP / WebSocket / 诊断 / 语音记录适配
├─ src/services/speechClient.js  # speech-to-speech 浏览器客户端适配
├─ src/services/desktopBridge.js # Tauri 与浏览器回退
├─ src/domain/viewModels.js      # Core 数据到 UI 数据的映射
├─ src-tauri/                    # Tauri 壳与 Core sidecar 生命周期
└─ tests/                        # UI 映射与 API 契约测试

services/core/
├─ src/xinyu_core/app.py         # FastAPI HTTP/WS 路由
├─ database.py / migrations/     # SQLite WAL 与迁移
├─ repository.py                 # 数据存取、FTS5、提醒数据
├─ services.py                   # 对话与显式记忆编排
├─ providers.py                  # 开发回退与 OpenAI-compatible LLM
├─ speech.py                     # Realtime 语音代理
├─ avatar.py                     # 人物状态机和视频资产探测
├─ reminders.py                  # 提醒、安静时段、温柔问候
└─ tests/                        # Core 回归测试

scripts/
├─ setup.ps1                     # 新机初始化
├─ dev.ps1                       # 启动 UI + Core
├─ test.ps1                      # 全量回归
├─ build-core-sidecar.ps1        # PyInstaller Core
├─ test-core-sidecar.ps1         # sidecar 健康检查
├─ setup-voice.ps1               # 语音独立环境
└─ start-voice.ps1               # 启动 speech-to-speech
```

## 7. 已实现 API（按领域）

| 领域 | 接口 |
| --- | --- |
| 运行与诊断 | `GET /health`、`/v1/runtime/status`、`/v1/system/capabilities`、`/v1/diagnostics`、`/v1/models` |
| 对话 | `POST /v1/chat`、`GET /v1/conversations`、`GET /v1/conversations/{session_id}/messages` |
| 记忆 | `POST /v1/memories`、`POST /v1/memories/query`、`GET /v1/memories/{id}/context`、`PATCH/DELETE /v1/memories/{id}` |
| 计划与通知 | `GET/POST /v1/plans`、`PATCH/DELETE /v1/plans/{id}`、`GET /v1/notifications`、`POST /v1/notifications/{id}/acknowledge` |
| 人格与情绪 | `GET/PUT /v1/persona`、`GET /v1/mood/current`、`POST /v1/mood` |
| 设置与数据 | `GET /v1/settings`、`PUT /v1/settings/{key}`、`GET/POST /v1/data/backups`、导入/导出/恢复/清除接口 |
| 语音与人物 | `POST /v1/voice/session`、`POST /v1/voice/transcripts`、`GET /v1/avatar/status`、`GET /v1/avatar/assets/{state}`、`POST /v1/avatar/state` |
| 实时事件 | `WS /v1/app/events`、`WS /v1/realtime/voice` |

详细字段以运行中的 `http://127.0.0.1:8765/docs` 为准。

## 8. 当前配置与真实模型接入方式

Core 支持这些环境变量：

| 变量 | 用途 |
| --- | --- |
| `XINYU_DATA_DIR` | SQLite、日志和运行资产目录 |
| `XINYU_AUTH_TOKEN` | 可选本地 Bearer Token |
| `XINYU_LLM_BASE_URL` | OpenAI-compatible 本地/受控 LLM 地址 |
| `XINYU_LLM_MODEL` | LLM 模型 ID |
| `XINYU_LLM_API_KEY` | 可选 Provider 密钥；严禁提交 Git |
| `XINYU_SPEECH_REALTIME_URL` | speech-to-speech 的 Realtime WebSocket 地址 |

未配置 LLM 时，系统使用明确标记的 `development-fallback`；未配置可用语音运行时，前端会诚实显示不可用原因而不会申请麦克风权限。

### 8.1 本机 16 GB 高质量模式建议

以下是候选和资源预算，**不是“下载后必然同时运行”的承诺**，必须先单模型、再双模型、再全链路基准测试：

| 模块 | 候选 | 运行策略 |
| --- | --- | --- |
| 中文 STT | SenseVoice / Paraformer；或 Faster-Whisper `large-v3-turbo` | 优先 CPU 或小 GPU 档；转写结束释放临时显存 |
| 本地 LLM | Qwen3 4B / 8B GGUF，经 `llama-server` | Windows 单机优先量化与显存可控；先 4B，再测 8B |
| 中文 TTS | Qwen3-TTS 0.6B CustomVoice | 首选 0.6B；1.7B 只在关闭实时口型或云端 LLM 档实验 |
| 人物口型 | MuseTalk 或等价方案 | 只处理约 256×256 脸部 ROI，不逐帧生成整张 1080p 场景 |
| 人物状态 | LivePortrait 离线生成 | 提前生成待机、倾听、思考、微笑、说话过渡等短视频 |

目标显存调度：

| 状态 | 优先常驻 | 应降级/释放 |
| --- | --- | --- |
| `idle` / `listening` | 硬件解码状态视频、可选小 STT | 口型模型休眠 |
| `thinking` | 量化 LLM | STT 转 CPU；继续视频硬件解码 |
| `speaking` | 0.6B TTS + Avatar Worker + 小 LLM | 限制 KV cache；队列过长立即降级到预制说话视频 |

验收目标：首个文本 token 尽量 < 1 秒、首段音频尽量 < 1.5 秒、收到首段音频后首个说话视频帧 < 800 ms、连续运行 1 小时无显存持续增长和明显音画漂移。没有达到这些标准时，优先缩小/卸载模型或切换预制视频，不降低人物资产质量。

## 9. 人物真正“动起来”的实施顺序

当前 UI 中的人物还不是会说话的数字人。实施必须按下面顺序，而非先上整套高负载模型：

1. **角色资产确认**：只使用有权使用的成年角色主图、表情参考、房间场景和声音；当前阶段不需要三视图，只有进入自由转身/全身动作/换装的 3D 阶段才需要。
2. **预制状态视频**：使用 LivePortrait 离线生产 `idle`、`listening`、`thinking`、`speaking`、`smiling`、`goodnight`。将文件放入：

   ```text
   <XINYU_DATA_DIR>/avatar/xinyu-main/
   ├─ idle.mp4
   ├─ listening.mp4
   ├─ thinking.mp4
   ├─ speaking.mp4
   ├─ smiling.mp4
   └─ goodnight.mp4
   ```

   前四个状态齐全时，Core 才会返回 `video_state_library`；浏览器会在首帧准备好后淡入，未准备好则继续显示静态图。
3. **接通真实语音**：先启动 STT → LLM → TTS 的 Realtime 运行时，向 `XINYU_SPEECH_REALTIME_URL` 配置其地址；验证打断、断线、转写、音频输出。
4. **局部口型 Worker**：仅在 `speaking` 加载/唤醒，使用 TTS 时间戳对齐帧；显存水位、帧队列或延迟异常时回退 `speaking.mp4`。
5. **长时测试**：测试插话、连续 1 小时、模型崩溃独立重启、显存回收和音画同步。

完整人物资产规格见 `docs/AVATAR_ASSETS.md`；完整高质量人物方案见 `docs/architecture/心屿-本地陪伴式智能体-技术可行性与开发文档.md` 第 10～11 节。

## 10. 测试、构建与交付命令

```powershell
# 全量：Core 测试、sidecar、UI build、UI test、Sites test、diff 检查
powershell -ExecutionPolicy Bypass -File .\scripts\test.ps1

# Core 单独测试
.\.venv\Scripts\python.exe -m pytest services\core -q

# UI 构建与单测
npm.cmd --prefix apps\desktop-ui run build
npm.cmd --prefix apps\desktop-ui run test:unit
npm.cmd --prefix apps\desktop-ui run test:sites

# Core sidecar
powershell -ExecutionPolicy Bypass -File .\scripts\build-core-sidecar.ps1
powershell -ExecutionPolicy Bypass -File .\scripts\test-core-sidecar.ps1
```

当前 Tauri 原生编译需要人工安装 Rust stable MSVC。安装后：

```powershell
cd apps\desktop-ui
npm.cmd run tauri:info
npm.cmd run tauri:dev
npm.cmd run tauri:build
```

## 11. 安全、隐私与发布门

- 所有用户数据默认保留在本地 SQLite；导入、恢复和清除前会创建安全快照并要求确认。
- 诊断报告只输出版本、状态、计数、组件和硬件摘要；不得包含消息正文、记忆正文、数据库路径、麦克风 ID 或密钥。
- 不把 `.env`、模型权重、SQLite、缓存、日志、声纹、真人素材或可执行 build 产物提交到 Git。
- 任何真人肖像、声音克隆、音乐、字体、模型权重、Windows 签名和云端密钥，都必须由项目负责人单独确认版权、授权与发布范围。
- `speech-to-speech` 按其 Apache-2.0 上游许可使用；其余第三方组件清单见 `THIRD_PARTY_NOTICES.md`。

## 12. 下一阶段任务清单（推荐优先级）

1. 在 RTX 4070 Ti SUPER 主机上建立模型基准表：STT A/B、Qwen3 4B/8B、Qwen3-TTS 0.6B/1.7B，记录首 token、首音频、显存峰值、中文效果和连续运行稳定性。
2. 基于基准结果实现 `ModelManager`：模型进程独立、显存水位监控、异常单独重启、状态切换时卸载不需要的模型。
3. 将 speech-to-speech 的真实运行时与当前 Core Realtime Proxy 接通，完成话筒到扬声器的完整打断回归。
4. 在获得合法素材后完成状态视频库，再加入脸部 ROI 口型 Worker。
5. 给长期记忆加入向量召回与摘要压缩，同时保留 FTS5 回退路径。
6. 安装 Rust、完成 Tauri 安装包与 Windows 通知/托盘/开机启动的真实验收。
7. 最后接日历、天气等外部授权服务，并进行隐私、许可证、素材来源和签名发布审查。

## 13. 现有文档索引

本文件是“对外转发的单一入口”。接手者开发时还应以以下文件为准：

- `docs/architecture/心屿-本地陪伴式智能体-技术可行性与开发文档.md`：完整技术可行性、架构、显存预算、人物路线、ADR、风险与验收。
- `docs/DEVELOPMENT.md`：环境、脚本、接口、数据与人工门。
- `docs/IMPLEMENTATION_STATUS.md`：逐项实现状态和自动化验证证据。
- `docs/TECH_STACK.md`：当前技术栈与可替换边界。
- `docs/VOICE_RUNTIME.md`：实时语音独立环境和 16 GB 档建议。
- `docs/AVATAR_ASSETS.md`：人物状态视频目录、编码与验收要求。
- `docs/PRIVACY.md`：隐私边界和数据流。
- `docs/ROADMAP.md`：里程碑顺序。
- `docs/HANDOFF_2026-08-01.md`：本次阶段性交接摘要。
